{
  "name": "file-generator-action",
  "version": "1.0.0",
  "description": "GitHub Action for generating files from HTTP response",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "axios": "^1.3.4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
} 